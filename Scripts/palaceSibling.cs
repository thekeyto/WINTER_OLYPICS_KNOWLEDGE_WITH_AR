using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class palaceSibling : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        transform.SetAsFirstSibling();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
